# ytlite secret 範本 —— 複製成 .env 後填入實際值
#
#   cp webbox/.env.example webbox/.env
#
# SERVER_SECRET_KEY 由 Invidious engine 與 companion 共用來互相認證。
# 這裡是單一來源：docker-compose.yml 用它同時餵給兩個服務
# （companion 讀 SERVER_SECRET_KEY，engine 讀 INVIDIOUS_INVIDIOUS_COMPANION_KEY），
# 所以不會再出現「兩個檔案各寫一份、改一邊忘另一邊」的問題。
#
# 長度必須剛好 16 字元（Invidious 在 config.cr:263 會檢查並 exit）。
# 產生方式：openssl rand -base64 12 | tr -d '=+/' | cut -c1-16
#
# 未設定時 compose 會直接報錯停住，不會靜默用預設值起來。

SERVER_SECRET_KEY=CHANGE_ME_16CHAR
